package game;

public class Enemy extends JixampleEntity{

	public Enemy(String PATH, String name, int tileX, int tileY, double friction, double acceleration, double maxSpeed) {
		super(PATH, name, tileX, tileY, friction, acceleration, maxSpeed);
	}

	@Override
	public void update() {
		
	}

	
}
